#!/usr/bin/env bash
# Mark a candidate promoted/rejected (append-only lifecycle). See mark.py.
set -euo pipefail
cd "$(dirname "$0")"
if [[ -f .venv/bin/activate ]]; then
  source .venv/bin/activate
elif [[ -f ../../.venv/bin/activate ]]; then
  source ../../.venv/bin/activate
fi
exec python mark.py "$@"
