httpx>=0.27.0
python-dotenv>=1.0.0
rich>=13.7.0
# optional access tiers (the ladder works without them; they light up if installed):
#   curl_cffi>=0.7.0      # stealth-GET rung (defeats TLS/JA3 walls e.g. Reddit)
# external CLIs (dispatch tier, not pip): gemini (YouTube), grok (X/Twitter), firecrawl (key)
